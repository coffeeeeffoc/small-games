import{describe,it,expect}from'vitest';import{applyChoice,realm,score}from'./model';describe('cultivation',()=>{it('applies bounded stats',()=>expect(applyChoice({body:1,spirit:1,luck:1},{text:'',result:'',delta:{body:-3}}).body).toBe(0));it('progresses realms',()=>expect(realm({body:20,spirit:20,luck:20})).toBe('飞升'));it('scores spirit highest',()=>expect(score({body:1,spirit:1,luck:1})).toBe(7))});
