import{useState}from'react';import{GameChrome}from'../../components/GameChrome';import{RewardedAdButton}from'../../components/RewardedAdModal';import{AdMode}from'../../services/adService';import{SaveData}from'../../services/storage';import{applyChoice,chapters,events,realm,score,Stats}from'./model';
export function CultivationGame({onBack,save,update,adMode}:{onBack:()=>void;save:SaveData;update:(f:(s:SaveData)=>SaveData)=>void;adMode:AdMode}){const [stats,setStats]=useState<Stats>({body:5,spirit:5,luck:5});const [idx,setIdx]=useState(0);const [log,setLog]=useState('你出生在一个下雨的夜晚。接生婆说：此子很会睡。');const [ended,setEnded]=useState(false);const [chapterIntro,setChapterIntro]=useState(true);const [fx,setFx]=useState<{id:number;text:string}[]>([]);const current=events[idx],chapter=chapters[current.chapter-1];function choose(i:number){const c=current.choices[i],next=applyChoice(stats,c);setStats(next);setLog(c.result);const text=Object.entries(c.delta).map(([k,v])=>`${k==='body'?'骨':k==='spirit'?'灵':'运'}${Number(v)>0?'+':''}${v}`).join('  ');setFx([{id:Date.now(),text}]);setTimeout(()=>setFx([]),900);if(idx===events.length-1){setEnded(true);const n=score(next);update(s=>({...s,coins:s.coins+Math.floor(n/3),bestCultivation:Math.max(s.bestCultivation,n),cultivationChapter:3}))}else{const nextIdx=idx+1;setTimeout(()=>{setIdx(nextIdx);if(events[nextIdx].chapter!==current.chapter){setChapterIntro(true);update(s=>({...s,cultivationChapter:Math.max(s.cultivationChapter,events[nextIdx].chapter)}))}},260)}}function restart(blessed=false){setStats(blessed?{body:6,spirit:6,luck:7}:{body:5,spirit:5,luck:5});setIdx(0);setEnded(false);setChapterIntro(true);setLog(blessed?'你带着一缕前世福缘醒来。':'轮回再启。这一次，你隐约记得前世的雨。')}return <GameChrome title="三分钟修仙" subtitle="三章十八劫 · 一世约 5 分钟" onBack={onBack}><section className={`cultivation game-stage chapter-${current.chapter}`}><div className="ink-sun"/><div className="cultivation-path">{chapters.map((c,i)=><div className={current.chapter>i+1?'passed':current.chapter===i+1?'current':''} key={c.name}><i>{current.chapter>i+1?'✓':i+1}</i><span>{c.name}</span></div>)}</div><div className="stat-strip"><span>骨 <b>{stats.body}</b></span><span>灵 <b>{stats.spirit}</b></span><span>运 <b>{stats.luck}</b></span><span>境 <b>{realm(stats)}</b></span></div>{fx.map(x=><div className="stat-float" key={x.id}>{x.text}</div>)}{chapterIntro&&!ended?<div className="chapter-curtain" style={{'--chapter-color':chapter.color} as React.CSSProperties}><small>第 {current.chapter} 章</small><h1>{chapter.name}</h1><p>{chapter.sub}</p><button onClick={()=>setChapterIntro(false)}>入此境 →</button></div>:!ended?<><div className="year-mark"><span>{current.age}</span>岁</div><article className={`story-card event-enter ${current.boss?'tribulation':''}`} key={idx}>{current.boss&&<div className="boss-stamp">章末劫难</div>}<small>第 {idx+1} 劫 / {events.length}</small><h1>{current.title}</h1><p>{current.text}</p><div className="choices">{current.choices.map((c,i)=><button onClick={()=>choose(i)} key={c.text}><span>{['甲','乙'][i]}</span>{c.text}<i>→</i></button>)}</div></article><div className="life-log">前情：{log}</div></>:<article className="ending-card ending-burst"><span className="seal">结</span><small>三章已毕 · 此世评价</small><h1>{realm(stats)}</h1><div className="big-score">{score(stats)}<small>道行</small></div><p>{log}</p><p>超过了 <b>{Math.min(99,42+score(stats))}%</b> 的修行者</p><div className="ending-actions"><button className="button primary" onClick={()=>restart()}>直接转世</button><RewardedAdButton mode={adMode} label="带着福缘转世" reward="下一世初始幸运 +2" onReward={()=>restart(true)}/></div></article>}</section></GameChrome>}
