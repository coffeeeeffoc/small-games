import{useEffect,useState}from'react';import{AdMode}from'./services/adService';import{SaveData,clearSave,loadSave,writeSave}from'./services/storage';import{CultivationGame}from'./games/cultivation/CultivationGame';import{OfficeGame}from'./games/office/OfficeGame';import{ArenaGame}from'./games/arena/ArenaGame';
type Game='lobby'|'cultivation'|'office'|'arena';
const games=[{id:'cultivation' as const,n:'三分钟修仙',en:'LIFE OF CULTIVATOR',tag:'3章18劫 · 奇遇抉择',desc:'用一炷香过完一世。每次失败，都给下一世留点东西。',icon:'仙',tone:'jade'},{id:'office' as const,n:'打工人摸鱼记',en:'OFFICE ESCAPE',tag:'5日生存 · 职场喜剧',desc:'老板转身就摸鱼，老板回头就装忙。熬到周末才算胜利。',icon:'摸',tone:'yellow'},{id:'arena' as const,n:'电子斗蛐蛐',en:'ODD CREATURE ARENA',tag:'5级联赛 · 自动战斗',desc:'孵出一只怪东西，一路变异，连续打穿五个离谱段位。',icon:'斗',tone:'pink'}];
export default function App(){const[game,setGame]=useState<Game>('lobby'),[save,setSave]=useState(loadSave),[mode,setMode]=useState<AdMode>('preview'),[settings,setSettings]=useState(false);useEffect(()=>writeSave(save),[save]);const update=(fn:(s:SaveData)=>SaveData)=>setSave(s=>fn(s));if(game!=='lobby'){const props={onBack:()=>setGame('lobby'),save,update,adMode:mode};return game==='cultivation'?<CultivationGame {...props}/>:game==='office'?<OfficeGame {...props}/>:<ArenaGame {...props}/>};const progress=[`${save.cultivationChapter}/3 章`,`${save.officeDay}/5 日`,`${save.arenaLeague}/5 段`];return <main className="lobby"><div className="grain"/><header className="lobby-head"><div className="brand"><span className="brand-mark">摸</span><div><b>摸鱼游戏社</b><small>POCKET ARCADE / 试营业</small></div></div><div className="head-actions"><span className="wallet">◈ {save.coins}</span><button className="icon-button" onClick={()=>setSettings(true)}>⚙</button></div></header><section className="hero"><div className="eyebrow">三个小世界，一次打开</div><h1>今天，<em>玩点怪的。</em></h1><p>三套完整关卡旅程：渡完十八劫、摸到周末，或者养一只怪物打穿联赛。</p><div className="ticker"><span>当前放映</span><div>修仙失败 / 带薪摸鱼 / 怪物互殴</div></div></section><section className="game-grid">{games.map((g,i)=><button key={g.id} className={`game-card ${g.tone}`} onClick={()=>setGame(g.id)} style={{'--delay':`${i*90}ms`} as React.CSSProperties}><div className="card-index">0{i+1}</div><div className="card-progress">进度 {progress[i]}</div><div className="card-icon">{g.icon}</div><div className="card-copy"><small>{g.en}</small><h2>{g.n}</h2><span>{g.tag}</span><p>{g.desc}</p><b>继续挑战 <i>↗</i></b></div></button>)}</section><footer><span>本地存档 · 无强制广告</span><span>v0.2 CAMPAIGN BUILD</span></footer>{settings&&<div className="modal-backdrop"><div className="modal-card settings"><span className="modal-kicker">开发者设置</span><h3>广告运行模式</h3><p>预览模式不会调用真实 SDK，只模拟等待后发奖。</p><div className="segmented">{(['preview','sdk','off'] as AdMode[]).map(m=><button className={mode===m?'active':''} onClick={()=>setMode(m)} key={m}>{m==='preview'?'预览':m==='sdk'?'B站 SDK':'关闭'}</button>)}</div><p className="hint">SDK 模式需配置 VITE_BILI_AD_UNIT_ID，并在 B 站客户端真机环境运行。</p><div className="modal-actions"><button className="button danger" onClick={()=>{clearSave();setSave(loadSave())}}>清空存档</button><button className="button primary" onClick={()=>setSettings(false)}>完成</button></div></div></div>}</main>}
