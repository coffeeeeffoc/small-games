# 卡丁车实时比赛使用可独立运行的服务

补充 ADR-0003 的部署拓扑：在管理服务和通用运行时服务之外，增加可选的 `services/kart-server`。好友联机需要持续运行的实时比赛循环，而本地电脑加公网隧道的使用方式不应依赖 PostgreSQL 或管理后台。

服务复用 carding-car 显式导出的纯 TypeScript 比赛逻辑，由服务端确定位置、碰撞、机器人、道具和名次。客户端经 WebSocket 上传操作，使用快照显示结果。房间和访客重连凭证仅保存在单进程内存；本阶段不做账号、持久化或跨进程调度。原有单机模式不依赖此服务。
