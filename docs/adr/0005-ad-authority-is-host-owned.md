# 广告控制权由 Game Host 在会话启动时确定

Game 只声明 Reward Opportunity；Game Host 以 `host`、`managed` 或 `none` 三种 Ad Authority 之一启动 Game Session，且会话期间不可切换或混用。这样 B 站渠道可以完全控制广告素材和完成校验，自有 Web/App 则可以使用运营配置，同时避免游戏代码耦合任何广告提供方。
