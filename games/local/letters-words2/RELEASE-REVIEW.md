# 2026-09-22：独立评审后第二轮修复

第一轮独立评审在冻结聚合版完成教材首批后发现：右上“× / 查看完成的棋盘”被 `.win-art` 装饰层截获，触摸后结算仍开着；下方回顾按钮可关，因此不是无法继续的严重阻断。协调者在第二轮将装饰层设为 `pointer-events:none`，保留原关闭事件和视觉样式；首屏“练习随时保存”改为“已完成的词会自动保存”，与实际恢复语义一致，不扩大存档承诺。

- 可运行回归补在已有 `tests/browser.test.mjs`：每次真实完成词单后，在原视口和 844×390 横屏触摸/点击右上关闭，再由收获入口重开，断言完成词存档字节不变。原来的底部关闭/键盘关闭/继续流程保留。
- 修改前对冻结 `43141/small-games/games/letters-words2/index.html` 运行，390px 真实触摸失败，日志明确为 `.win-art intercepts pointer events`：`.scratch/six-games-95/words-result-close-red.log`。首次尝试旧43513端口已停止，仅属服务不可达，随后使用正确冻结入口复现。
- 修改后 `pnpm --filter ciyu-word-tiles build`、`check` 通过；当前 `dist` 在43514运行，完整浏览器检查1280/390/320/305及各自横屏关闭、全屏/异常分支全部通过，日志 `.scratch/six-games-95/words-result-close-green.log`。仍为 Windows Chrome 153 桌面和触控模拟，非手机真机。
- 最新 `dist/app.js` SHA-256 `2DF6ACBEEB944DCD0B117317794C0E2667BC1662A98D6550CD8FCF89D7DDEC91`，`dist/styles.css` `BDF3D5E081611A1B913CF0BA423A65BC3EC3F1C5C6DEEAB2E0390D0420D7FA96`。引擎、教材、存档结构和共享全屏源未改。随后 Shell R2 整合 18/18 通过，新独立 R2 评审确认两项修复及核心学习流程，原值 8.650，仍未达到 9.5；详细证据和评审方法限制见 `INDEPENDENT-REVIEW.md` 顶部。R3 集成时逐文件确认本游戏与 R2 相同。

---

# 2026-09-22：9.5 冻结量表第一轮实现记录

本轮基线重新构建、实际操作后建立，未沿用下方历史评分。实现者 `/root/implement_words`，独立评审尚待进行；本节分数仅适用于 Windows Chrome 153.0.8010.50 的桌面及触控模拟，**尚未达到 9.5，也不代表真实用户均分或全面上线验收**。量表按 `docs/plans/2026-09-22-six-games-95.md` v1 冻结。

## 入口、目标玩家与发布边界

- 目录 `games/local/letters-words2/`；源码 `index.html`，独立发布产物 `dist/index.html`；Shell 游戏 ID `letters-words2`。本轮独立构建服务 `http://127.0.0.1:43513/`，由 `python -m http.server 43513 --bind 127.0.0.1 --directory dist` 提供。
- 已明确目标是手机 H5 与桌面 Web。当前没有此游戏的独立原生小游戏适配产物，不新增微信/B站目标；Shell iframe 由总负责人集成验收，不能以独立入口代替。
- 面向按教材练词的学生及轻度英语复习者。核心是看中文、选择完全露出的字母、整词消除解锁下层；同字母可互换，错误不扣命，可以免费提示/重排。无计时失败和实时对局；本轮失败链路对应错误拼写→修改→再试，暂停对应离开操作/帮助遮罩→返回继续。
- 无业务后端、登录和联机服务。教材 JSON 为同源按册资源，Web Crypto 做 SHA-256 校验，IndexedDB 缓存教材，localStorage 保存完成词及复习记录。下载教材要求安全上下文（HTTPS 或 localhost）；首次离线启动、存储被系统回收后的恢复不可宣称支持。

## 全新基线与当前评分

| 维度 | 权重 | 基线 | 当前实现建议 | 本轮依据与分数边界 |
| --- | ---: | ---: | ---: | --- |
| 核心玩法与操作乐趣 | 30% | 8.3 | 8.3 | CUA 从中文海洋拼 ocean、错误 sm 修正 Ms、I 完成整岛；四列布局未改变严格遮挡和整词消除。仍缺真实玩家对中后段决策乐趣的观察，不为布局改动加玩法分。 |
| 首局理解与教学 | 20% | 8.2 | 8.2 | 根据中文和亮牌可完成首词，错误反馈明确；未增加实操教学，儿童无外部解释的独立完成率未验证。 |
| 移动操作、视听与反馈 | 20% | 7.4 | 8.8 | 基线 CUA 320 视口/305 内容宽字母约 29.14px，没有全屏入口；新版同条件 45.33px，305 触控回归核对实际无阻挡点击跨度≥44px。全屏按钮随页面滚动仍可访问，提示/弹窗退出可用。实体 Android/iOS、工具栏与安全区仍未验收。 |
| 重玩或持续学习 | 15% | 8.6 | 8.6 | 本轮前后都重新完成新版 Welcome 31 词/6 批/1 词尾批；易错词复习与缓存单元切换可用。未修改学习内容，不加分；跨日学习迁移和复玩意愿未验证。 |
| 功能完整与稳定性 | 15% | 8.7 | 8.9 | 当前构建的四视口、全屏与旋转保留未完成拼写，取消触摸不选牌，旧完成词记录恢复；仍缺发布 Shell、公网与真机的全链路证据。 |
| **加权原值** | **100%** | **8.205** | **8.515** | 不经四舍五入判断，当前不通过 ≥9.5 / 每维≥9 的门槛。 |

真实用户满意度/评分区间：**无法可靠估计**，没有目标玩家样本、线上留存或分享数据；专家实玩与自动功能回归不构成用户评分证据。

## 本轮两项实际改善

1. **窄屏字母可点区域增大**。排布由 560 宽六列变为 360 宽四列，仍保留 18 个基础槽位、相同字母大小和严格遮挡规则；桌面棋盘上限 360px。玩家收益是 305px 可用宽度不再点约 29px 小牌。通过条件：可取牌实际点击跨度≥44px，无越界；80 牌/长词、重复字母、符号和全局可解；布局切换不重排。均已通过下述检查。代价是棋盘增高，页面需要纵向滚动；实际点击能到达提示、撤销、拼写、词单和结算，没有用裁切缩放塞进一屏。
2. **接入现有公共全屏控制**。`index.html` 提供始终可达的手机顶部全屏入口，`build.mjs` 和 `server.mjs` 公开并复制根负责人维护的 `fullscreen.js`。请求完整文档及 HUD；帮助/教材/结算原生弹窗内有公共退出入口。通过条件：普通→全屏→操作→弹窗退出→继续，旋转保持选择和进度，不支持/拒绝后准确反馈仍可玩。当前桌面 Chrome 实际 API 和模拟失败分支通过；实体手机尚未验证。

## 当前运行证据

- 本轮前：重新 `node build.mjs`，再运行原 `tests/browser.test.mjs` 三视口和 `tests/library.browser.test.mjs`，全部通过。CUA 实玩当前基线 ocean→刷新保留 1/6；320 视口截图与 DOM 实测确认小牌问题。基线 `dist/engine.js` SHA-256 `8EEDC80E47A5FCCD3D1FB0FE0BA51FAD1A2B9892090D8511A1FF491DD192D337`。
- 本轮后：`pnpm test` 通过，包含 320 个完整随机对局、0 次手动救济、80 牌/短语边界；48 词库、26 核验教材/313 分组及源字节和哈希检查通过。未变更教材字节、来源和导出哈希。
- `pnpm check`、`pnpm build`、`git diff --check -- games/local/letters-words2` 通过。
- `$env:GAME_URL='http://127.0.0.1:43513'; node tests/browser.test.mjs` 通过：1280×960、390×844、320×740、305×740；实际点击/触控覆盖遮挡、错误、撤回、清空、切词、完整通关重玩、16 字母词布局、重复与符号、刷新续练、结算回开。新增真实 Fullscreen API 进入/退出、844×390 旋转、帮助弹窗退出保持 partial selection；305px 下逐个可取牌用 `elementFromPoint` 验证 44px 的水平/垂直有效跨度；CDP touchStart→touchCancel 后选牌状态不变；缺失/拒绝 API 为明确标注的模拟分支。无页面/本地资源错误。
- 同址 `node tests/library.browser.test.mjs` 新版通过：真实触控完成孙有中三上 Welcome 31 词、6 批和 1 词尾批，每批关闭再打开结算继续；首词刷新恢复、原教材大小写、31 提示词复习、已缓存 Unit 2 断网启动。此测试使用提示，不推断真人难度/学习效果。
- 新版 CUA 亲自从旧六列存档恢复已完成 ocean；强制刷新最新资源后，320 视口/305 内容宽可取牌为 **45.328125×45.328125px**。导入 `I 我 / Ms 女士`，拼 sm 出现错误且不吞牌，清空后 Ms→I 通关；关闭结算、返回“查看本岛收获”、进入仅含 Ms 的 1 词易错复习均成功。Chrome 扩展操作全屏请求被浏览器拒绝时出现准确提示，并可继续完成；这条是实际拒绝路径证据，不能冒充全屏成功。
- 当前产物基于根 `eeb877d75df163a27287162c7d48f27f6cbc8184` 加未提交改动。`dist/engine.js` SHA-256 `0FD381239FFA99464B04C6F62D1961DF26225B16EA7B23CFC946C8C1E1F742CC`；`dist/index.html` `3E6AA40A93EF21D84196F414C02CD3046DABF79F0A7DEC546ED24B4C8DED6399`；`dist/styles.css` `4FA9D19955F06968A1DBDF875D6E7D8ACB83B1D4F855BFF58478E3D35C66FBEF`。共享脚本如后续同步，须由集成轮重记 hash。未提交、推送、部署。

## 尚未达标项、阻塞与爆款验证

基线前三问题：① 约 29–31px 字母触点；② 独立 H5 无全屏入口；③ 缺目标学生首局理解与跨日学习证据。前两项已修复并在限定浏览器验证；第三项仍待真实观察。当前无已复现而未修复的核心流程阻断，但没有 Android/iOS 实体浏览器、真实学生和上线访问数据，不能全量验收。

当前前三优先项：实体小屏完整全屏/降级与误触观察；学生是否理解遮挡及提示后的记忆；跨日继续教材的主动意愿。解除阻塞最少需要 Android Chrome 与 iPhone Safari 各一台、同一当前 H5 构建的 HTTPS 可达入口、对应年级学生的知情同意观察。独立实现未验证 Shell iframe，等待总负责人集成。

辨识度来自中文提示+层叠字母与真实教材单元；目标清楚，消一词即有解锁回报，下一局由单元进度与易错词驱动。分享/邀请的自然理由尚未被证实，不能把教材数量当增长证据。优先做两个小实验：① 5–8 名对应年级学生，不讲解观察首词/首岛、误触与提示依赖，再隔一天复拼同一易错词；② 家长/班级作业群的小规模自愿入口测试，只记录实际开始、单元完成、次日主动回访和转发后开始，不预设结果。先复用已有宿主统计与观察表，不扩建分析平台。发音/语境/间隔复习是待验证方向，只有观察显示相应学习瓶颈才实施。

---

# 历史归档：词屿上线优化复核

2026-09-21，第一轮实施；等待独立复审，不以开发者自评代替 8 分验收。

## 已完成

- 保留任意相同字母可用、任何面积遮挡不可选、整词正确才移除的规则。共享提交函数在正确拼词后检测余牌，无可拼词时自动免费整理，并在界面解释原因。
- 真实教材选择与按册 SHA-256 下载校验、IndexedDB 缓存。按完整教材单元分批练习，支持短语及最后一批单词。
- 教材 / 当前词单完成进度刷新恢复，易错词和提示词结算复习。存储禁用时仍可玩。
- 320px 的释义不再与状态提示重叠；教材标题换行，单列词单与至少 40px 操作区。

## 验证证据

- `pnpm --filter ciyu-word-tiles test`：320 个完整随机对局，正确操作后零次手动救济；重复字母、严格遮挡、符号、短语、80 牌、单词尾批及恢复记录校验。
- `pnpm --filter ciyu-word-tiles check` 与 `build` 通过。
- `node games/local/letters-words2/tests/browser.test.mjs`：独立 Chrome headless context，1280×960、390×844、320×740。真实点击 / 触控覆盖遮挡、错误拼写、撤回、切词、提示、重复符号、完整通关重玩、导入校验、刷新恢复；无页面 / 本地网络错误和横向溢出。

## 已合并的教材与数据边界

历史 22 份词表、9,974 条记录保留，仅提供整册。最终合并外研 PDF 核验教材 26 册、5,588 条、313 个学习分组：陈琳 2011 课标小学 8 册与初中 6 册；孙有中 2022 课标小学三至五年级上下册及六上，初中七八年级上下册及九上，共 12 册。新版优先显示，册次、主编和课标版本明确区分。

原书 Module 是旧版小学词表的单元粒度，没有将其猜分至课内 Unit 1/2；旧版初中按原页码映射 Module/Unit，新版按原表 Welcome/Starter/Unit 标题。每条保留 PDF 页，目录保留原来源、SHA-256 和收录范围。本批官方资源没有新版六下与九下，明确未收录，不以旧版替代。

已运行 `vocabulary:sync`、`test`、`check` 与 `build`。库检查覆盖每册下载哈希、与共享源逐字节一致、单元隔离、完整分批；根真实浏览器验证旧版三上 Module 2 的选册、开始、完成首词、刷新进度。`test:library:browser` 用真实触控完整完成新版三上 Welcome 31 词、六批及一词尾批，验证续练、大小写、提示词复习和已缓存册断网练习；独立评审再次运行通过。完整证据由 `INDEPENDENT-REVIEW.md` 记录。

根补修了教材显示大小写，I、Ms、China、Monday 保留原样；匹配仍不区分大小写。加入经过游戏、存档、复习的回归，以及1280/390/320三种视口真实点击完成 I/Ms、刷新核对与易错词复习。来源和完整印次放入可展开的原生详情，默认保留版次选择与学习范围。

## 2026-09-21 第二轮：结算关闭后的教材续练

本节是本轮新增证据；基线为 `510df72` 的六游戏优化及 `95d45d1` 的教材字节保留修复。上文及 `INDEPENDENT-REVIEW.md` 的 8.2 分是这些基线已有的历史结果，保持原样。本次实现者的估分不是用户平均分，本轮修改交给另一名代理重新独立复评。

### 实际发现与最小修复

在独立服务 `http://127.0.0.1:43103/`、320×740 Chrome 触控模拟中，真实导入并完成 `I 我 / Ms 女士`，点击“留在这里，回顾词单”后，结算关闭且所有词已完成。界面可用按钮仅剩音效、帮助、选教材、换一组及导入，没有返回结算的入口。教材批次使用同一弹窗，关闭后无法从完成棋盘继续下一批或进入易错词复习；刷新虽可重新打开，但界面没有提供这条恢复路径。

复用现有 `showWin()`，在完成棋盘的拼写区显示“查看本岛收获 · 继续练习”按钮。返回原结算即可续练、重玩或复习，保留教材和易错词状态；新牌局自动隐藏按钮。实际产品改动为两行 JavaScript、一行 HTML，没有增加状态、依赖或改动教材字节。

### 本轮评分建议

| 维度 | 权重 | 本轮修改前 | 修改后建议 | 依据 |
| --- | ---: | ---: | ---: | --- |
| 核心玩法与玩家行动 | 30% | 8.2 | 8.2 | 叠放、严格遮挡、重复字母与完整拼词消除的有效循环保持。 |
| 首局理解 | 20% | 8.2 | 8.2 | 当前词义、可拼状态、提示和错误反馈清晰；本轮没有加文案冒充教学提升。 |
| 手机操作与反馈 | 20% | 7.8 | 7.8 | 三视口可操作、无横向溢出；320px 实测字母牌宽约 30.84px，窄屏精确点按仍有改善空间。 |
| 重玩与学习价值 | 15% | 7.3 | 8.3 | 修复关闭弹窗后丢失的续练与复习入口，保留真实单元批次与提示词复习。 |
| 完整性与稳定性 | 15% | 7.6 | 8.7 | 新增关闭结算、键盘 Esc、重新打开、教材六批续练的回归通过。 |
| **加权总分** | **100%** | **7.9（原值 7.875）** | **8.2（原值 8.21）** | 历史独立评分是 8.2；本轮额外发现路径缺口，因此修改前重新估计，修复后不擅自抬高旧分。 |

### 本轮重新执行的验证

- `pnpm --filter ciyu-word-tiles test`：320 个引擎完整对局、零手动救济；48 册教材、26 册核验教材 / 313 分组的哈希、源字节与分批检查全部通过。
- `pnpm --filter ciyu-word-tiles check`、`pnpm --filter ciyu-word-tiles build`：通过。
- PowerShell 设置 `$env:GAME_URL='http://127.0.0.1:43103'` 后运行 `node games/local/letters-words2/tests/browser.test.mjs`：1280×960、390×844、320×740 通过。保留原有遮挡、错误拼写、撤回、提示、完整通关、刷新恢复、大小写与布局验证，新增关闭结算后重新打开、Esc 关闭后键盘 Enter 返回，以及重玩后入口隐藏的检查。
- 同一地址运行 `node games/local/letters-words2/tests/library.browser.test.mjs`：390×844 真实触控完成孙有中新版三上 Welcome 的 31 词、六批与一词尾批。每一批都关闭结算、从完成棋盘重新进入结算，接着成功续练；末批返回后成功进入 31 个提示词复习。首词刷新恢复、教材大小写、已缓存单元断网练习也通过，无页面错误。

### 距离爆款仍缺的证据

窄屏密集字母约 31px，需要实体小屏设备的误触率与单词完成耗时数据，再决定是否改变牌局密度。当前没有发音、词组语境或跨日掌握度；也没有真实学生的首局完成率、次日留存和分享转化，不能从内部 8.2 分推断爆款。本轮未验证实体手机、微信内置浏览器或首次离线启动，未修改原始教材和来源哈希。
