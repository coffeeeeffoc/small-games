# 验证记录

## 2026-09-14：转向修复与地图调整

- 复现旧逻辑：按右方向 0.5 秒，静止时转角为 0，倒车时转角为 -0.9565。新增 `scripts/check-steering.mjs` 在修改前因静止转向失败，修复后通过。
- 保留速度对高速转弯的影响；取消倒车反转转向和静止禁用转向。骑行、步行、停放摩托统一由场景模块同步坐标与旋转，修正停放摩托及加速尾气的方向符号。
- 新增中央花坛、咖啡/花园方向的错位窄口、环岛海岸路。共享地图数据驱动 3D 道路、建筑、花坛碰撞与大小地图，地图同步显示圆形海岸和码头。
- `pnpm test` 通过：包括原有订单/输入检查、方向键与 WASD 到游戏更新的集成检查、正反向/静止/刹停/撞墙/角度跨界，以及真实场景碰撞体的地图检查。海岸环路采样 720 点均可通行，13743 个可达网格连接全部订单、取货点和码头。
- `pnpm build` 通过；新增代码不引入依赖，生产 JS gzip 约 142.86 KB。
- 生产预览 `localhost:43118`、844×390 横屏：浏览器输入左右箭头各 0.4 秒，静止转角分别约 +0.529/-0.553，倒车时约 +0.762/-0.722；左右含义一致。
- 用输入事件实际完成集市下车、三件拣货、返回原停车位、骑行绕过中央花坛和咖啡错位弯道、交付结算。未修改角色坐标或阶段；83 秒、100% 完整度、三星、49 金币。控制台无警告/错误。
- 以上是桌面 Chromium 的键盘事件和模拟触屏 PointerEvent 验证；真实手机多指操作仍需设备复测。

## 历史验证

2026-09-12，Windows / Codex Chromium 浏览器。真实手机硬件与 iOS Safari 尚未测试。

## 可重复的逻辑检查

`pnpm test` 使用 Node 自带 assert / EventTarget，无额外测试框架。

- 完整接单、近距离交接、三件货物、上车、成功结算；无重复金币。
- 墙体碰撞、损货失败、超时、重试、非法时间步、跳跃与冲刺能量。
- 圆形海岸、步行/骑行边界、码头抵达与侧边约束。
- 多指同时操作和互不串扰；pointercancel / lostpointercapture / blur 复位。
- 跳跃只消费一次；按钮聚焦时空格保留原生行为；销毁移除监听。

## 实际浏览器流程

生产构建运行于 localhost:43118，844×390 手机横屏尺寸。

- 通过页面按钮接单，以输入事件驱动角色移动（没有修改角色坐标或跳过状态），实际依次到达集市、面包、橘子、牛奶、停车位、街道中转点、海风咖啡。
- 完成一单：43 秒，100% 货物完整，三星，53 橘子币（含 15 小费）；点击下一单正确进入灯塔订单。
- 最终构建再次实际完成全部拣货，验证回到原停车坐标及朝向后上车；灯塔路线 23 秒、100% 完整度、三星，获得69金币。两条路线均通过真实界面按钮交付。
- 暂停前后倒计时保持相同；地图可打开关闭；真实浏览器按空格起跳，观测跳跃高度大于零。
- 模拟两个触屏 PointerEvent 同时操作摇杆和加速：速度 7.88，boosting=true，能量从100下降至82.08；取消后boosting=false、steer=0。浏览器工具不支持系统级原生多触点注入，系统指针捕获与多指手感仍应真机复测。
- 刷新后保留累计1单、53金币与设置。
- 使用浏览器网络离线模拟后重新载入，首页和 3D 小镇正常恢复；Service Worker 已控制页面并存在版本化缓存。测试结束已恢复联网。
- 修复试玩发现的集市门牌遮挡、下车摩托消失、牛奶摊标签不一致；添加主要建筑的相机遮挡收近。
- 667×375 与844×390横屏布局检查通过，无页面横向溢出；390×844竖屏正确显示旋转提示。
- 该场景约9.3–10万三角形、60–85 次 draw call；首屏代码和样式压缩传输合计约148 KB（构建报告 gzip 估算），无额外模型/音频下载。此数据不是低端真机性能保证。

## 已知范围

订单在刷新后重新开始，只保存已完成收益。金币用于累计成绩；尚无消费商店或联网排行。交通车辆与行人是环境装饰。iOS 全屏、设备振动和 PWA 安装行为依浏览器支持而定。
