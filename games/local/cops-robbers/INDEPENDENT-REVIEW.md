# 2026-09-23 独立联调验收：本地与公网真实双客户端通过

评审者 `/root/review_cops_letters` 未参与实现。先打开当前游戏、点击进入 PK 并完成实际浏览器对局，之后读取 `RELEASE-REVIEW.md`。入口 `http://127.0.0.1:43010/games/cops-robbers/`，真实 PostgreSQL 测试库由协调者运行；测试账号仅进入 `competition_test`。这份记录不把旧报告、接口脚本或 SDK mock 当本轮 UI 通关。

| 本人实际执行项 | 结果与证据 |
| --- | --- |
| 390×844 单人专注 | 开局即显示棋盘、步数和操作，宣传内容退出主要区域；棋盘 x=13、y=143、364×364，在屏幕内。 |
| 双独立玩家完整比赛 | 两个 Chrome context 分别取得服务器签发的不同身份；创建→真实复制邀请链接→第二会话打开链接并读取房间码→加入→双方准备→各 13 步 Canvas 点击围捕→等待对方→两端得到一致结算→双方进入同一新房再来一局。另覆盖手输房间码。没有直接发送动作 API、注入胜负或修改状态。 |
| 排位与结算 | 当前固定地图 17，两端成绩均为 13 步；真实返回个人最佳、人数、名次和相邻目标，结算页面可见。没有由此推断已完成 101 人边界/持久化恢复测试；那部分由协调者另列证据。 |
| 浏览器全屏 | 独立单人页面和 PK dialog 的按钮均实际进入/退出浏览器 Fullscreen API，PK 状态不变；不是 CSS 充满。真实 iframe `Permissions-Policy: fullscreen=()` 拒绝时显示中文反馈，`fullscreenElement` 保持为空。 |
| 环境边界 | Windows 安装版 Chrome、390×844 浏览器视口；本地与临时 HTTPS 隧道分别实测，不是手机真机或微信/B站开发者工具证据。CUA 子线程无法 attach 页面，因此使用可重复的浏览器验收脚本。 |

复跑：仓库根执行 `node scripts/review-competition-cops-letters.mjs cops-robbers`；`--focus` 检查专注与全屏；可用 `$env:REVIEW_BASE` 指向同构测试网关。证据为 `outputs/independent-cops-letters/cops-robbers-{playing,waiting,results}-a.png`、对应 JSON，以及 `report-*.json`。路线由已有求解器计算用于验收，所有游戏动作仍由真实 Canvas 点击触发；不把已知路线回放当陌生玩家自主乐趣。

## 本轮独立发现

| 编号 / 级别 | 复现、影响及通过条件 | 当前状态 |
| --- | --- | --- |
| C1 / P1 | A 在围捕创建房间，B 在词屿填同一码并加入。B 显示“这个房间属于另一款游戏”，A 却显示 2/2，合法好友无法再加入，B 也无法在当前 UI 准备或退出这个房间。房间 `1708569D6698`；截图 `cops-robbers-crossgame-crossgame-a.png`、`letters-words2-crossgame-crossgame-b.png`。通过条件：服务端按期望游戏在占座前拒绝，原房仍 1/2。 | 已关闭：新产物中 B 被拒绝，A 下一次真实轮询仍 1/2。`--failures` 通过。 |
| C2 / P2 | 等待房间期间浏览器断网，点击“退出 PK”；弹窗仍打开，状态为英文 `Failed to fetch`，退出依赖网络恢复。截图 `cops-robbers-offline-exit-crossgame-a.png`。通过条件：本地可退出并可继续单人，准确说明服务端退出未确认，恢复后处理原房间。 | 已关闭：断网点击后 dialog 立即关闭，外部显示“已退出 · 房间待确认”，单人画面可用；恢复网络后从入口取回同一房间并正常退出通过。`cops-robbers-offline-exit-fixed.png`。 |

评分继续使用冻结权重 30/20/20/15/15、目标总分 ≥9.5 且每维 ≥9。局部 H5 PK 通过不抬高历史专家分 8.84，也不代替六端完整性、真机或真实新玩家证据；本轮整体仍未通过 9.5 和平台硬门槛。

## 公网专项复验（2026-09-23）

通过临时 HTTPS 隧道，由两个独立 Chrome context 真实完成：返回大厅找 PK→创建→复制邀请→第二会话打开链接并加入→准备→并行各 13 步 Canvas 点击→一致结算与全站排名→点击结算面板实际可见的“再次挑战”→进入同一新房。比赛 `1A990CCE5D6F`，服务端用时分别 **9.569 / 9.052 秒**，个人名次 **20 / 19**；新房 `CCFAD440C741`。浏览器真实全屏进入/退出保持局面。没有点击被结算覆盖层遮住的旧按钮，没有直接提交动作或分数；本次成功运行的页面及请求错误记录为空。

首次公网运行的 5 秒创建等待上限未取得房间，随后调整测试等待为 15 秒并保留说明后重跑通过；不能把这次等待超限认定为游戏后端缺陷。PowerShell profile 曾延迟命令返回，后续运行使用无 profile 模式，不通过重启服务掩盖问题。

本地证据保持不动，公网证据单独保存于被 Git 忽略的 `outputs/independent-cops-letters-public/`：`report-cops-robbers---parallel-players.json`、两端 `cops-robbers-results-*.png/json`。临时域名仅记录在该本地证据中，不写入提交的配置或本文。复跑设置 `REVIEW_BASE` 为当前隧道 HTTPS 入口、`REVIEW_EVIDENCE_NAME=independent-cops-letters-public`，执行 `node scripts/review-competition-cops-letters.mjs cops-robbers --parallel-players`。

---

# 围捕小队：冻结量表 v1 独立评审 R1（2026-09-22 历史结论）

评审者 `review_kart_cops_r1` 未参与产品实现。先按可见界面自由操作并保存 `first-impressions.md`，之后才读取本轮实现报告；下方旧评分没有作为本轮基准。**本轮独立评分原值 8.84/10，未通过 9.5 门槛。未发现新的确定产品缺陷，但缺目标新玩家、跨日复玩和实体手机证据。** 这是限定环境的专家判断，不能估计上线玩家均分。

## 评审版本与证据边界

- 仓库起点 `eeb877d75df163a27287162c7d48f27f6cbc8184` 加未提交工作区；测试冻结制品为 `.scratch/six-games-95/review-build`，没有提交、推送或部署。
- 聚合入口 `http://127.0.0.1:43140/small-games/#/games/cops-robbers`；独立入口同服务 `/small-games/games/cops-robbers/index.html`。原交接端口 43432 已停止，因此不把它列为本轮实际入口。
- 冻结制品 `src/main.js` SHA-256 `287bbc88ccf985b1bee065872f7facab7b6a521e3659b5b93b17f9e8b5e95181`；`src/style.css` 为 `3a632ee3b545297e0059719c86a2b7ab7e42dbbe6326be5567478cb49f9cd1c4`，与工作区文件一致。公共全屏脚本 `4451eded9653669ae98fd27e9dc3eff43d41c14286ec66a27d99629f18a34828`。
- Windows Chromium 自主逐次决策，Chrome/Edge 回归；390×844、320×740、305×700、844×390，独立浏览器上下文。鼠标、键盘、CDP 触摸是真实浏览器输入；这些仍是桌面仿真，不是实体手机或人类目标玩家。
- 本轮证据总目录 `.scratch/six-games-95/review-kart-cops-r1/`，包含 `artifact-hashes.json`、`independent-observations.json`、首屏/305/横屏结算/第二关失败截图，以及保留可运行副本的测试脚本。副本仅适配绝对导入、输出目录和冻结预览子路径，不改产品规则。

## 实际体验与复测

1. **先自由体验**：以慢速逐条阅读七步首关教学，实际完成守右出口、守左出口、接防、收紧、留守、合围，7 步三星。旋转到横屏后真实点击下一关成功。第二关自行安排守口和包抄，遇到非相邻落点时明确拒绝且不耗步；错误包抄后真实失败，弹窗指出“第 3 步，1 号小偷从 3 号路口逃到 4 号出口”。撤销至第 1 步，刷新仍是第 1 步；提示只高亮建议、不替玩家行动。第二关没有记录为无提示自主通关。
2. **输入**：独立 CDP 双指扰动未产生额外回合；拖出再拖回原点保持第 0 步并显示取消；touchCancel 仍为第 0 步，之后正常点路口到第 1 步。慢输入、错误输入、快速双击、动画中重开/选关、键盘、提示竞态、拖动预览均由当前制品的 20 项回归再次通过。
3. **胜负、恢复、内容**：20/20 交互回归通过，含失败撤销/重试、部分抓获不提前胜利、损坏/拒绝存储、旧存档迁移和音效启停。代表关 1/17/49/60 分别以 7/13/14/16 步真实点击通关；首关和密集末关在 390/320 宽触摸仿真通关。后者使用已知参考路线，是内容与规则回放，不是自然策略或陌生玩家乐趣证据。
4. **显示**：305 宽无横向溢出；独立页原生全屏目标为整个 HTML，失败弹窗也有退出入口。聚合页实际全屏目标是同时包含工具栏和 iframe 的 MAIN，内页退出会同步清除父页全屏。横屏胜利继续/重玩通过。回合制无实时计时压力，刷新恢复已验；实体设备后台、系统回收及工具栏变化未验。

回归过程有两处测试适配问题，不能算产品缺陷或隐藏绿灯：原脚本 `goto(base)` 缺尾斜线在 Vite 子路径预览落入 Shell fallback；补为明确目录入口后重跑通过。原 standalone `serve.mjs` 的 404/405 私有路径断言不适用于 Vite fallback，副本明确跳过该服务边界检查，**没有声称冻结预览复验了 standalone 服务安全策略**。初次日志和修正后日志均保留。

## 五维评分：当前证据，不记为产品增分

| 维度 | 权重 | 分数 | 依据与尚不足 9.5 的原因 |
| --- | ---: | ---: | --- |
| 核心玩法与操作乐趣 | 30% | 8.8 | 守口、接防、合围与失守因果在自由操作成立；末关回放可靠。尚无早中后段多种自主策略及代表玩家持续乐趣观察，不能据参考解推到 9/9.5。 |
| 首局理解与教学 | 20% | 8.8 | 七步实操覆盖关键规则，错误、撤销和提示能帮助自救；第二关自由尝试仍失败并使用提示。没有真实首次玩家无需讲解完成并迁移的证据。 |
| 移动操作、视听与反馈 | 20% | 9.0 | 在本轮桌面仿真范围内，窄屏、旋转、拖回/取消、多指、全屏和音效反馈无阻断；Android/iOS 手指遮挡、安全区、工具栏变化和真实降级尚未验证。 |
| 重玩或持续学习 | 15% | 8.6 | 三星、最佳步数、60 关边界、下一关和存档可用；代表内容回放不等于玩家形成省步策略，更没有跨日主动回来玩的观察。 |
| 功能完整性与稳定性 | 15% | 9.0 | 当前本地关键正常/异常路径与完整交互回归通过；9 分仅限明示测试环境，实际目标移动发布环境仍未通过验收。 |
| 加权原值 | 100% | **8.84** | `8.8×0.30 + 8.8×0.20 + 9×0.20 + 8.6×0.15 + 9×0.15`；不满足总分及全部维度要求。 |

实现者新基线 8.13、实现后估分 8.525 保留作为其口径。本评审没有运行修改前制品，不量化本轮修改带来的分数增量；8.84 与其估分的差异是独立现状证据判断，**不是玩法/重玩提升**。拖回取消、横屏继续和窄屏保存失败提示的当前结果已独立复验，这些具体修复可以确认，不能由此给未变化内容加分。

## 仍需处理的事项与通过条件

- **确定可编码问题：本轮未新增。** 不因分数未达标追加装饰、奖励或新关卡；若下一轮出现明确误触/迁移阻碍，再按复现修最小路径。
- **待目标玩家确认的体验假设**：七步跟随是否能迁移为第 2/3 关自主换防，密集后段是否仍能辨清落点，最佳步数是否构成主动重玩理由。玩家影响可能是照着点却不会判断、后段误触或只玩一次；现有代理和参考路线不能确认影响发生率。通过条件：首次目标玩家无口头解释完成并自主应用守口/接防，记录误触与失败自救；代表中后段自主游玩及跨日返回观察支持同一量表，不能拿脚本通过率替代。
- **外部环境待验证**：至少目标 Android Chrome 与 iPhone Safari 的当前实际制品，在独立/聚合入口完成全屏或准确的不支持提示、退出、横竖屏、取消、多指、后台重入、存档和持续游玩。通过条件是留存设备/浏览器/制品身份与实际操作证据；本轮没有这些设备证据。该游戏已有 H5 实现，不因此新增声明微信/B 站原生支持。

---

以下为历史报告，不是当前评分：

# 围捕小队：2026-09-21 历史第二轮独立复评（非本轮结论）

日期：2026-09-21。评审：`review_pursuits_round2`，未参与产品实现，本轮只更新评审报告。对象是当前 `dist`，通过 `http://127.0.0.1:43422/` 访问。本轮重新操作、重新评分，下方旧报告仅为历史。

## 本轮结论：8.3 / 10，通过约定的体验门槛

这是基于当前浏览器构建的专家体验评分，不是已测得的用户平均分，也不能据此承诺爆款。采用统一权重；未发现本轮实测范围内的严重游玩阻断，各维度均高于 6。

| 维度 | 权重 | 本轮得分 | 独立评价 |
| --- | ---: | ---: | --- |
| 核心玩法与玩家决策 | 30% | 8.4 | 守出口、调度接防、原地留守、合围都影响小偷下一步，误调守口警察会真实失守。策略因果成立，但后段难度尚未经过陌生玩家验证。 |
| 首局清晰度 | 20% | 8.4 | 亲自跟随七步教学获得三星；不仅指定走法，也解释守口与接防的目的。离开教学路线仍能自由尝试，并用失败解释和撤销恢复。 |
| 移动体验与反馈 | 20% | 8.2 | 390×844 的首关教学、触摸危险预览与取消可用；320×740 的终章可点路口、查退路、撤销且无横向溢出。终章人物、编号、路线更密集，仍有视觉辨识成本。 |
| 重玩与学习价值 | 15% | 7.8 | 60 关、五街区、三星步数和个人最佳提供明确目标；目前主要是有限关卡与更少步数，缺少被验证的长期回访与分享理由。 |
| 完整性与稳定性 | 15% | 8.5 | 本轮亲测获胜、失败、撤销、重开、触摸取消和多人关卡；独立规则检查通过，两个浏览器测试页未记录 warn/error。稳定性结论限于所测浏览器范围。 |

加权：`8.4×0.30 + 8.4×0.20 + 8.2×0.20 + 7.8×0.15 + 8.5×0.15 = 8.285`，显示为 **8.3**。这里的 8 分是本次约定的体验门槛，不等于已完成商店发行或实体设备验收。

## 本轮亲自取得的可复现证据

1. **构建身份**：本地 `src/main.js` 与 `dist/src/main.js` SHA-256 相同；HTTP `/src/main.js` 内容与本地 `dist/src/main.js` 完全相同。CodeGraph 未索引此目录后，回退直接审查源码，重点核对 `updatePlanning`、拖动事件、`cancelDrag` 和 `loadLevel`。
2. **完整首局**：浏览器已有存档，先通过“重新开始”回到第 0 步。在 390×844 中逐步按页面教学操作：1 号到 2、3 号到 5、2 号到 10、3 号到 1、1 号留守 2、1 号到 7、2 号到 9。最终页面显示“1 名小偷全部归案，用了 7 步”和三颗星。这只证明教学路线可操作、说明与结果一致，不拿已知路线推断陌生玩家的独立解题难度。
3. **本轮触摸预览改动**：第 1 步后用 Chrome 的真实触摸输入事件，从位于 2 号路口的 1 号警察拖向 7 号路口，页面出现“走到 7 号后：2 号出口会失守！”。保持按下并拖回 2 号路口，恢复“若原地留守：5 号出口会失守！”。发送触摸取消后仍为第 1 步，没有提交移动。
4. **真实失败与恢复**：上述局面点击 7 号路口，确实在第 2 步从 2 号出口逃脱；失败框解释“1 号离开出口前，需要队友接防”。点击“撤销这一步，换个走法”，恢复第 1 步、1 号警察在 2 号路口、教学 2/7，可以继续操作。
5. **窄屏和多人说明**：以 `/?level=60` 进入终章，页面准确显示“4 人协作：你动 1 人，3 名小偷都会行动”。在显式 320×740 视口中，文档滚动宽度 305，不超出可用宽度；查小偷退路、触摸 9 号路口和撤销均实际完成，撤销后回到第 0 步。未声称手动通关终章。
6. **独立执行检查**：在游戏目录运行 `node scripts/check-levels.mjs`，结果为 60/60 解法、60/60 必要小队、60/60 真实逃脱、60/60 当前局面提示通过；共 692 次单警行动、40/40 分批抓捕检查通过。该脚本验证规则和解法，不等于 60 位真实玩家通关体验。

上述 UI 证据由本轮 CUA 实际操作取得；IAB 的触摸 CDP 不支持，触摸部分改用 Chrome 完成。本轮未重跑实现者的 18 项交互全套或全部浏览器关卡回放，其记录不计作本轮亲测。

## 若以爆款为目标，仍需验证和优化

- 优先观察陌生玩家离开七步教学后的第 2、3 关：能否独立理解换防，错误后是否读懂预览，提示是否被反复依赖；再决定是否增加可跳过的换防小挑战。
- 对 320px 的多人终章做实体手机手指遮挡、密集角色选中和编号辨认测试，根据误触率决定是否放大选中人物或增强退路层次。
- 先用首局完成率、次关到达率、失败后重试率和次日回访验证内容吸引力，再选择每日同题、短解法分享等具体回访机制；当前没有真实留存或传播数据支撑爆款结论。

**边界**：本轮是桌面 Chrome/IAB 的手机尺寸模拟，触摸事件也不是实体手机。未测低端 Android、iOS Safari、微信/哔哩哔哩实际运行容器、音量与触觉体感；未做陌生玩家可用性测试或线上留存分析。

---

## 以下为第一轮历史报告（保留原文，不代表本轮新增证据）

# 围捕小队：独立复评

日期：2026-09-21。评审：`review_pursuits`，未参与实现，只写此报告。评审对象为当前本地 `dist` 构建；结论不是线上用户均分或爆款预测。

## 结论：8.3 / 10，通过本轮首发体验门槛

| 维度 | 权重 | 得分 | 当前版本依据 |
| --- | ---: | ---: | --- |
| 核心玩法与玩家决策 | 30% | 8.5 | 每次只移动一名警察，小偷随即行动，守口、留守与换防都有实际后果。独立重跑证明 60 关每名警察都不可缺，关卡不是靠多余棋子堆难度。 |
| 首局清晰度 | 20% | 8.3 | 亲自完成首关七步教学：守右口、守左口、接防、内收、留守、两侧合围。每步说明目的，错误走法仍可执行，失败后能撤销学习。 |
| 移动体验与反馈 | 20% | 8.1 | 390×844 地图、教学、小队和撤销/提示均可用；合法落点、人物移动、危险出口和动画反馈连贯。320px 经典滚动条边界问题已修复并复测。 |
| 重玩与学习价值 | 15% | 8.0 | 60 关、五街区、三星步数、个人最佳形成清晰短目标；第二关仍需自己判断守口顺序，提示不会代替玩家执行。 |
| 完整性与稳定性 | 15% | 8.5 | 胜利、真实逃脱、撤销、提示、重开及读档构成完整流程；规则与交互回归通过。仍缺实体设备和真实玩家数据。 |

加权：`8.5×0.30 + 8.3×0.20 + 8.1×0.20 + 8.0×0.15 + 8.5×0.15 = 8.305`，显示为 **8.3**。各维度均不低于 6，复测后未发现阻断正常游玩的严重问题。

## 独立操作与验证

Chrome 自动化连接故障后使用独立 IAB 标签页，确认标题为“围捕小队 · 街区追逃计划”，访问本地构建 `http://127.0.0.1:43422/`。浏览器已有测试进度，主动选择第 1 关并在设置重开教学，不把已有成绩当作自己的结果。

- 首关第一步派 1 号到 2 号出口，实际看到小偷从 6 号到 1 号；下一步故意留守，得到“第 2 步从 1 号路口逃到 5 号出口”的真实失败。撤销恢复，再让 3 号守 5 号出口。
- 按教学完成剩余接防与合围，真实结算为 7 步、三星，显示个人最佳及下一关/重玩入口。
- 第 2 关自行守上方出口，再使用当前局面提示。提示选中 3 号并指出 5 号落点，点击才执行；刷新后仍恢复第 2 步的警察、小偷和出口危险局面。
- 人工检查 390×844、320×740。首次在 Windows 经典滚动条下发现 `clientWidth=305 / scrollWidth=320`，回交修正后重载最新构建，复测为 **305 / 305**。
- 独立执行 `pnpm --filter cops-robbers test`：60/60 解、692 次移动、60/60 全队必要性、实际失败与当前局面提示均通过。
- 独立执行 `test:interactions`：当时 15 项全部通过，含快速输入、拖动取消、提示竞态、损坏/拒绝存储、音效、键盘与读档。修复者补充的 305/320px 回归后为 16 项通过；新增窄屏问题另由本评审实际复测。

复测构建：`dist/src/main.js` SHA-256 `D01FBDFB383B53A0B8E13ADFB227B0D45FCFD74C12259BD620ECDBF0FBC63455`；`dist/src/style.css` SHA-256 `F4ADA42026F0A07EA722E90D96987D159BCDAA863B1AB9E9226B4DDA6624E811`。

## 非阻断的后续空间

首关以后的教学迁移还可以更细：在首次遇到多人小偷、四警协作时，用一句局面提示解释新难点。先观察真实新手的失败点，再决定是否增加，避免每关直接透露解法。长期回访目前主要依赖有限关卡和步数纪录；8.3 分不证明留存或传播能力。

本轮人工样本为首关完整胜负和第二关局部流程，未声称亲自打完全部 60 关。窄屏是浏览器视口验证，触摸自动回归也不等于实体手机验收。未发布到公网。

重新启动预览：先运行 `pnpm --filter cops-robbers build`，再在仓库根目录执行 `$env:PORT='43422'; pnpm --filter cops-robbers preview`。此评审启动的服务只保证评审活跃期间可用。
