# 围捕小队：首轮优化与验收

日期：2026-09-21。基线体验评分 7.5/10；本轮为实现记录，最终评分由另一位 agent 独立复评。

## 发现与实现

- 首关只有规则段落，新玩家难以从“守口、换防”转为实际操作。现在复用已验证的首关参考解，按当前真实局面标出警察与目标路口，并解释七步中的守口、接防、留守、合围。可在设置关闭；首次通关自动关闭，重玩不再自动泄题；偏离路线不会限制自由操作或提示错误落点。
- 原来的橙线没有说明推演条件。现在明确显示“若原地留守”或“走到 X 号后”，危险时直接报出下一步会失守的出口。
- 失败文案原来笼统提示“提前守口”。现在重放最后一步的真实引擎结果，显示小偷从哪个路口逃到哪个出口；确实因为守口警察离岗而失守时指出接防问题。仍可一键撤销或重试。
- 复用现有星级与最少步数存档，在局内、结算和选关显示个人最佳、三星步数和差距，以及街区三星进度。手机教学和威胁文字加大，未增加新的遮挡面板。

本轮只改本游戏的 `index.html`、`src/main.js`、`src/style.css`、`scripts/check-interactions.mjs`。未修改引擎、关卡、协作必要性或计分门槛。

## 验收证据

开发服务：`http://127.0.0.1:43422`，独立 Chrome 会话人工检查 390×844、320×740。

| 检查 | 结果 |
| --- | --- |
| `pnpm --filter cops-robbers test` | 60/60 参考解、692 次单人移动；60/60 去掉任一警察不可解；60 个实际逃脱失败；60 个当前局面提示均通过 |
| `$env:BASE_URL='http://127.0.0.1:43422'; pnpm --filter cops-robbers test:interactions` | 15 项通过：教学跟随移动/撤销、实际出口/离岗复盘、教学与最佳纪录持久化、单步输入、胜负、提示竞态、双击、拖动取消、重开、损坏/拒绝存储、音效、键盘、动态效果设置 |
| 同一 `BASE_URL` 下 `pnpm --filter cops-robbers test:browser` | 60 关真实浏览器点击通关；390×844 与 320×740 触屏首关及末关通关，首屏控件可用且无横向溢出 |
| `pnpm --filter cops-robbers build` | 构建通过 |
| `git diff --check -- games/local/cops-robbers` | 无空白错误 |

浏览器验收记录与截图在本地忽略目录 `outputs/escape-report.json`、`outputs/escape-interactions.json`、`outputs/escape-mobile.png`。人工实际执行首关守右出口、错误留守导致第 2 步从 1 号路口逃到 5 号出口、撤销恢复、选择另一位警察守左出口及换防，界面状态与规则一致。

## 仍需独立评价的边界

- 以上为本地浏览器与触屏模拟验证，尚未获得真实玩家评分、留存数据或物理手机验收。
- 教学只覆盖首关，其他关仍依赖局面观察与已有提示；独立复评需检查是否足以让玩家理解换防，而不是只会照着点。
- 重玩目标使用已有 60 关与三星/步数纪录，未增加账号、排行榜、奖励系统或新关卡。
- 本轮未提交、推送或发布；线上页面仍是原版本。

## 第二轮：触屏预演与协作迁移（2026-09-21）

本节是当前实现者的增量记录，保留上面的首轮证据；独立复评另行执行。基线为 `95d45d1`，没有把旧版 60 关浏览器回放当作本轮验证。

### 当前发现与实现

- 已有鼠标悬停能显示目标路口的后果，但拖动只绘制连接线，手机玩家松手前看到的仍是“若原地留守”。新增触摸输入回归在修改前实际失败。现在拖动和松手共用原有 58 单位落点判定，复用规则引擎预演；拖回起点、空处松手、取消或失去指针捕获时清理目标预览，只有有效松手才执行一步。
- 第 17 关引入两名小偷，第 49 关增加第四名警察，第 57 关出现三名小偷。手机布局隐藏左侧任务卡，这些关开局原来只有通用操作提示。现在复用现有说明区，按真实人数说明“你动 1 人，所有小偷都会行动”、守口换防和点小偷查看退路；首关七步教学仍优先显示，刷新进行中的关卡仍恢复续局操作提示。
- 只修改 `src/main.js` 和现有 `scripts/check-interactions.mjs`，没有改变捕获规则、关卡、参考解、星级或存档结构。

### 本轮验证

预览 `http://127.0.0.1:43422` 确认为 `node scripts/serve.mjs --dist`，本轮构建后的 `src/main.js` 与 `dist/src/main.js` SHA-256 相同。

| 检查 | 本轮结果 |
| --- | --- |
| `pnpm --filter cops-robbers build` | 通过，10 个发布文件 |
| `$env:BASE_URL='http://127.0.0.1:43422'; pnpm --filter cops-robbers test:interactions` | 18 项全部通过；新增两项分别验证真实 Chrome 触摸事件的落点预演/松手/移回起点/取消，以及 320px 下多偷/四警开局提示与续局 |
| 同一 `BASE_URL`，`$env:LEVEL_IDS='1,17,49,60'; pnpm --filter cops-robbers test:browser` | 4 个代表关分别 7/13/14/16 步通关；390×844、320×740 首关和末关触屏通关；无页面/控制台/资源错误 |
| `git diff --check -- games/local/cops-robbers` | 通过 |

真实触摸回归在第一步守住 2 号出口后，拖动守卫离岗时显示“走到 3 号后：2 号出口会失守”，松手前双方位置和步数不变；移回起点后预演恢复原地留守，取消后仍为第 1 步。检查过截图 `outputs/escape-touch-preview.png`；构建版报告为 `outputs/escape-interactions.json` 和 `outputs/escape-smoke.json`。最后的回到起点检查曾因 CDP 事件尚未绘制而过早断言，已改为等待可见预演文字，再完整跑过交互检查。

### 实现者估分与剩余优化

以下为体验启发式估分，不是真实用户平均分，也不是独立复评结果。统一权重为核心玩法 30%、首局理解 20%、移动反馈 20%、重玩动力 15%、稳定性 15%。

| 维度 | 本轮修改前 | 修改后估分 |
| --- | ---: | ---: |
| 核心玩法 | 8.3 | 8.3 |
| 首局理解与规则迁移 | 8.1 | 8.3 |
| 移动操作与反馈 | 7.9 | 8.3 |
| 重玩动力 | 7.8 | 7.8 |
| 稳定性 | 8.5 | 8.5 |
| 加权总分 | 8.1 | 8.3 |

成为爆款仍缺真实新玩家的独立解题率、次日回访和传播证据。下一步优先让首次玩家自由玩到第二关及双偷关，观察是否只会照提示走；若普遍卡住，再补对应概念教学。现有 60 关和三星步数目标足以开始验证重玩，不增加未经验证的养成或排行榜。上述验收为桌面浏览器和触屏模拟，未代表物理手机测试；本轮未提交、推送或发布。
