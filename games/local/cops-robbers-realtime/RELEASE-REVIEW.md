## 2026-09-25 双角色与 300 关更新

本节为当前验收；下方旧 48 关记录属于历史版本，不代表新模式已经在物理手机或原生小游戏验收。

- 三模式各 100 关；每模式的去折点路网指纹均 100 个，包含镜像/细分去重检查。全部道路连通、正交，渲染交叉路口与引擎拓扑一致。
- 新挑战 81–100 为 40–45 路口、7–8 环、7 追逐/8 突围，两座外院包含真实队员、守卫、出口和移动指令。逻辑地图 2000×900，保留道路物理长度。
- `pnpm test`：26 项通过，包括 200 个自由模式场景的静止突围队被 AI 合围，以及合法移动获胜样例。出生点不能靠相互堵路形成无法进入的永久保护区；出口竞速即使突围先动 2 秒，均存在及时到达出口的追逐队员。
- `pnpm test:levels`：100 次真实命令获胜见证，100 次不操作失败。末 20 关见证为 32.5–87.2 秒。前 48 关另有 198 次单队员尾追失败、缺员/换防对照；未把这些额外对照结论套用到后 52 关。
- `pnpm test:review`：真实触摸开始、自然失败、复盘、返回准备、重新挑战；突围队下令、追逐 AI 运动、失焦暂停与显式恢复，手机模拟宽 390 像素通过。
- `pnpm test:mobile-roster`：320×740 / 844×390，末关 8 人按钮全部可见且至少 44×44；320 像素下空 F1 路口的点击正确下达给当前队员，不再选中旁边角色；刷新保留模式/角色/先动偏好。
- 失败复盘已独立标为 `review`，清楚说明本局停止计时，并显示返回/重试。界面角色、教程和地图提示采用追逐队/突围队。本地头像从共享装扮模块绘制，好友服务不接收头像文件。

源码单机与上述 Chrome 触摸模拟已验证。宿主好友赛、数据库和发布构建由主任务单独验收；以上结果不代替微信/微博内置浏览器、物理手机或原生平台实测。

---

# 别跑！街区围捕 — 2026-09-23 共享后端接入交接

本节为当前实现 agent `realtime` 的交接；根仓库协调者负责身份、HTTP 房间、PostgreSQL、分享、平台打包及独立评审。旧报告不代表本轮在线或原生验收通过。评分门槛保持加权 ≥9.5、各维 ≥9.0；没有新增玩家研究或真机证据，本次不抬高旧评分。

## 现状矩阵与改动边界

| 项目 | 当前代码 | 本次验证 | 未完成/验证责任 |
| --- | --- | --- | --- |
| 目录/入口/渲染 | `games/local/cops-robbers-realtime`，`index.html`，`dist/index.html`；真实 Canvas 2D 主画面，DOM 工具与弹窗 | 读取 `engine.js`、`main.js`、`renderer.js`、`levels.js`，CodeGraph 当前未索引本目录 | 不是纯 DOM 或 iframe 引擎 |
| 桌面 Web / 手机 H5 单人 | 原 48 关、实时命令、双警练习、个人存档保持 | 22 项 Node 检查、窄屏/练习/全屏专项 | 实体 Android / iOS 未验证 |
| 专注界面 | 开始/练习后按 playing/paused 状态切换；移走品牌、长说明、侧栏，保留任务/时间/进度/地图/警察/留守/地图返回/声音/全屏/规则/暂停 | 320×740、390×844、844×390 无横向溢出、不依赖页面滚动，核心按钮高度 ≥44px；暂停继续仍同局 | Shell/iframe 由协调者验收 |
| 全屏 | 保留既有公共 `src/fullscreen.js`，本次不复制新控制器；专注布局不依赖 API 成功 | 当前 Chrome 真正进入 HTML 全屏、触摸下令、暂停、弹窗退出全屏后同局继续 | App 内浏览器/真机及 iframe 由协调者验收 |
| 好友 PK / 主榜规则 | 新服务端 `services/runtime-api/rules/realtime.mjs` 复用原物理，公开同图独立双人挑战；`src/competition-renderer.js` 接共享 room.state | 真规则+Canvas 触摸完成围捕、重连 JSON 恢复与不同查询频率最终一致；没有模拟两玩家变量冒充联网 | 真实 HTTP 两身份、PG 持久化和榜单由协调者集成 |
| 微信 / B站 | 新纯 Canvas renderer 不调用 DOM 或 SDK，供协调者独立原生入口；绘制复用已有角色/路线画法 | 浏览器真实 Canvas 触摸；没有执行 SDK mock | 两个独立平台目标与真实开发工具运行由协调者记录，本文件不宣称原生通过 |
| 后端依赖 | 现有 runtime-api 与 PostgreSQL、稳定 competition v1；此游戏无需新进程/数据库 | 规则引擎无需网络/DOM，静态模块可由 Node 直接载入 | 平台账号/密钥/合法域名和生产资源统一清单 |

## 排位协议与规则

- `id=cops-robbers-realtime`、`version=street1-physics120-v1`，固定第 1 关“岔路初见”、初始警察/小偷/出口、行为随机种子和速度。房间 seed 不改变榜单难度，同一榜所有玩家条件相同。
- 准备后 120 秒限时；每位玩家独立指挥同一街区，不互相操纵警察。复用原来的道路寻路、身体阻挡、翻越出口、至少双警就位、所有可退避路段封闭后维持 0.8 秒收网。逃脱一个即失败。
- 只接受 `{type:'move',cop,x,y}` 或 `{type:'hold',cop}`。校验警察下标、有限坐标、地图范围、可达道路、严格字段；拒绝客户端 score、elapsed、捕获数量等字段。玩家身份/成员资格/请求序号与房间生命周期由共享层校验。
- `advance(state, elapsedMs)` 按服务端时间在原引擎推进整数 120Hz tick；共享层在房间 GET/POST 锁内对两玩家推进。`realtime=true`、`pollMs=250`。查询频率不影响物理结果；无查询期间不需要逐房间定时器，下次访问按真实已过时间补算，最多本局 120 秒。断线继续执行已有命令，不能暂停或回拨时间。
- `view` 返回当前地图、角色位置、我方路线、出口状态、真实收网诊断、捕获数和用时；不下发测试解法、随机种子或小偷未来路线。客户端只保存选中警察和命令反馈。
- 结果 `score = 全部抓获 ? 1000 + 捕获数 : 捕获数`，高分优先，再比较 `secondary=服务端终局毫秒`，低用时优先。同图只有一名小偷，合法完成记录为 1001 分，核心可比值是用时。完全相同成绩依共享层并列规则。失败/超时/未完成/离线练习不入全站榜；无虚构名次或本机榜替代。
- 当前自动化工具可复现合法命令；服务器能拒绝越权/造分、非法移动和跳时，不能识别人类与辅助程序。固定图解法可能被学习或自动执行，不承诺绝对防作弊。规则、地图或物理变更需升级 version 隔离历史记录。

## 当前实际验证与续接

从本游戏目录执行（Windows / PowerShell）：

```powershell
node --test tests/*.test.mjs
node scripts/verify-levels.mjs
node scripts/build.mjs
$env:PORT='43123'; node scripts/serve.mjs
# 另一终端，服务使用当前 src；检查 dist 时加 --dist
$env:GAME_URL='http://127.0.0.1:43123'
node scripts/guided-playtest.mjs
node scripts/competition-playtest.mjs
node scripts/playtest.mjs
```

- Node 22/22 通过。新增的单一规则检查涵盖非法输入、真实放任逃脱、原策略合法获胜、不同查询频率、每次命令后 JSON 序列化恢复、同一物理终局用时、结束后拒绝操作和隐藏未来路线；没有直接注入胜负。
- `guided-playtest.mjs` 4 项通过，报告 `2026-09-22T16:49:48.609Z`（本地 9 月 23 日）。保留同局练习返回、慢操作/误留守后恢复、305/320/390px、真实 Fullscreen API 与弹窗退出后继续。
- `competition-playtest.mjs` 5 项通过，最终报告 `2026-09-22T16:59:53.536Z`：真实 Canvas pointer 输入选警察、点道路，服务端规则以 19,333ms 完成；320×740、390×844、844×390 专注布局按钮可见且高 ≥44px，暂停恢复；收到共享 `competition-visibility` open 事件时暂停原单人而不弹窗遮住 PK，close 后出现原暂停页且须明确继续。该脚本是浏览器输入与真实规则的直接集成检查，**不代表 HTTP/PG 双客户端、隧道或平台 SDK 验收**。
- 截图/JSON 在被 Git 忽略的 `artifacts/competition-canvas-390.png`、`competition-canvas-report.json`、`online-before-390.png`、`online-after-390.png`、`guided-report.json`。全部是桌面 Chrome 手机尺寸模拟，非真机。
- 运行现有全量浏览器回归时遇到一次练习等待胜利超时，再次通过练习后在忙碌主机上命中固定 500ms 等待不够（采样约 14 FPS）。没有改游戏时间规则；该断言改为等待模拟实际前进 0.3 秒（仍有 25 秒超时且检查小偷实际位移），避免把调度耗时当游戏静止。最终全量 `playtest.mjs` **16/16 通过**，`errors=[]`，报告 `2026-09-22T16:57:58.610Z`；含真实失败重试、首两关鼠标/触摸获胜、存档重载、键盘/拖动取消、18 关换防。
- `verify-levels.mjs` 完成 48/48 合法通关、48/48 无人指挥失守、198/198 单警尾追失败及缺失任意警员不能取胜检查；`build` 与 `git diff --check` 通过。此轮没有重跑旧生命周期全套，已在专项里覆盖与本次变动直接相关的暂停恢复、窗口容器切换和全屏。
- 最后构建 SHA-256：`dist/src/main.js` = `0615FC4F1878FB04173960F0312B787D76032B501EBB2376AB8342B5950B1913`；`dist/src/competition-renderer.js` = `143BFCC35FA9A803142D5CD2401E0969332400C4F3ED9E131A31097F76D11E6A`；`dist/src/style.css` = `D2EFB191C81C787A3BD0349CA596F9164C548623715581658A287F4F5F234F60`。

下一步由新的独立评审 agent 先操作新构建与共享在线入口，再阅读本总结。必须补齐真实两客户端同一结算、PG 重启成绩仍在、101 玩家榜外名次、隧道 HTTP/WSS（本游戏自身用 HTTP）及微信/B站开发工具矩阵；当前实现不能宣称整轮硬门槛或 9.5 已达标。

---

# 别跑！街区围捕 — 2026-09-22 冻结 9.5 量表实现交接

本节为当前证据；下文旧报告仅为历史。新独立评审尚未执行，以下是实现者按 [冻结量表 v1](../../../docs/plans/2026-09-22-six-games-95.md) 重新实玩及回归后记录的内部暂评分，不是玩家评价，也不是上线许可。

## 本轮基线与当前结论

| 维度 | 新基线 | 当前 | 当前依据与剩余扣分 |
| --- | ---: | ---: | --- |
| 核心玩法与操作乐趣 30% | 8.5 | 8.5 | 实时封口、改令、双警夹击有因果反馈；手动完成练习，正式首关手动封口，当前浏览器规则回归完成 1/2/18/48 关。后段是策略脚本驱动输入，不能证明目标玩家觉得好玩或多种策略均易发现。 |
| 首局理解与教学 20% | 8.0 | 8.6 | 慢速或误按留守使两警停下后，现明确指出哪名警察应继续追击；跟提示用真实触摸命令能收网。双警练习迁移到岔路、换防仍缺陌生玩家观察。 |
| 移动操作/视听反馈 20% | 8.0 | 8.8 | 305/320/390px 无横向溢出，文字全屏入口至少 44px，滚动仍可见；真实 Chromium Fullscreen API 下可操作、暂停、退出并继续同局。未验 Android/iOS 实体设备、安全区、工具栏变化与低端机。 |
| 重玩/持续学习 15% | 8.4 | 8.4 | 48 张路网、7 个换防关、挑战星、个人用时与进度恢复保留且复验；没有跨日复玩或难度流失证据，因此不因提示增多加分。 |
| 完整性/稳定性 15% | 8.6 | 8.9 | 中途复习现保留本局位置、命令、选中警察和时间，返回显式继续；主链路、失败重试、存档、取消和离线回归通过。整套目标设备的发布验收未完成。 |
| 加权原值 | **8.300** | **8.625** | 未达到 9.5，且各维未全部达到 9。 |

真实上线用户均分：**无法可靠估计**。无真实玩家样本、留存或分享数据。不存在可将本次代理评估转换为用户均分区间的证据。

## 产品、入口与基线问题

- 面向喜欢短局指挥、路线判断的轻度策略玩家。核心为先封逃脱岔路、再从不同方向逼近，并在需要时换防；48 关、每局约数十秒的规则回放，不等同真实新手完成时长。
- 目录 `games/local/cops-robbers-realtime` 是主仓库本地游戏，不是子模块。独立入口 `index.html`；`scripts/build.mjs` 复制成 `dist/index.html` 与 `dist/src`。Shell 登记于 `apps/shell-web/src/standalone-games.json`，ID `cops-robbers-realtime`，聚合路由 `#/games/cops-robbers-realtime`。
- 已有明确发布形态为桌面 Web / H5 静态产物；横屏优先，竖屏可玩。该目录没有微信/B站或其他小游戏原生产物、适配入口；本轮不扩平台，不作原生验收结论。
- 无账号、好友房或联机，无外部网络服务；音画本地生成。`localStorage` 保存已过关和个人最佳，本局原先不跨刷新存储，清除浏览器数据会清除进度。中途复习的保留仅在当前页面内。
- 当前测试入口为 `http://127.0.0.1:43122`，明确运行最新 `dist`；非旧开发服务。聚合/iframe 集成由总负责人单独验证，不能由本文件的独立入口验证代替。

本轮最影响体验的三个基线问题：

1. 实玩慢速按教学两次下令后，有时两警停在尚未封闭的间距，原提示只有“继续压缩包围”。玩家不知道到点会停或该再操作谁。已改为给较远警察下令的具体指导；未更改速度、警员数或捕获规则。
2. 暂停复习原入口明确写“结束本局，练习夹击”，返回会重开该关。这是有提示的旧产品行为，**不是存档丢失 bug**；现改为“暂停本局，练习夹击”，保留局面，降低复习成本。
3. 旧全屏只有图标，手机尺寸被缩到 37px 或 34px，滚动后离开视口。现接入公共脚本、文字入口和吸顶栏；原生 dialog 内也有全屏/退出入口。

## 实际修改与通过条件

| 修改 | 玩家收益 | 可运行通过条件 |
| --- | --- | --- |
| `main.js` 保存练习前内存局面，退出及练习获胜共用返回 | 复习不必重做本局封口 | 时间、警察/小偷位置、命令、出口状态、选中警察完全一致；返回暂停且显式继续后时间推进 |
| 两警均停止且未闭合时给出具体警号与重复追击提示 | 误操作后可恢复教学，不在原地猜规则 | 慢速操作+误按留守后出现提示，按提示真实触摸可以收网，不注入状态/胜利 |
| 共用全屏脚本接入、44px 文本入口、窄屏吸顶 | 游戏中可发现全屏、退出并继续原局 | 305/320/390px 无溢出；滚动仍可触达；真实全屏 → 下令 → 暂停 → 弹窗退出 → 继续，状态不重建 |

没有新增依赖、付费服务、排行榜或原生平台；未提交、推送、部署。`src/fullscreen.js` 为总负责人统一同步的共享副本。

## 本轮运行记录

当前工作区基于主仓库 `eeb877d75df163a27287162c7d48f27f6cbc8184`，含本轮未提交修改。最终构建文件 SHA-256：

- `dist/src/main.js`：`91C886E34B49487748EF9B0E035619DE2F39BD33E97F72BFFF23F4284AC75F44`
- `dist/index.html`：`0DFB92862DE94871BBB8B2A7954A375C61B654B28186686BB262CA95C76CF238`
- `dist/src/fullscreen.js`：`4451EDED9653669AE98FD27E9DC3EFF43D41C14286EC66A27D99629F18A34828`

游戏目录中实际执行：

```powershell
node --test tests/*.test.mjs
node scripts/verify-levels.mjs
node scripts/build.mjs
$env:PORT='43122'; node scripts/serve.mjs --dist
# 另一终端
$env:GAME_URL='http://127.0.0.1:43122'
node scripts/playtest.mjs
node scripts/lifecycle-playtest.mjs
node scripts/guided-playtest.mjs
```

- 核心 21/21 通过；规则回放 48/48 通关、无人指挥 48/48 失守、单警尾追 198/198 失败。引擎和 48 关数据本轮未修改。规则回放是可完成性证据，不能充当自然玩家体验。
- 浏览器回归 16 项通过，`errors=[]`，报告时间 `2026-09-21T18:49:49.152Z`。包含真正失败/回看/重开、首两关真实输入胜利与继续、保存重载、18 关换防、触摸横竖屏与取消。18 关使用解锁存档夹具和既有策略驱动输入，非自然玩家通关。
- 新增 `test:guided`（4 项）先在旧构建明确失败：返回复习后 `ready !== paused`；实现后通过，报告时间 `2026-09-21T18:48:55.378Z`，`errors=[]`。包含同局保留、慢输入/误按后恢复、305px 布局、真实 Chromium 全屏/暂停/退出/继续。保留可运行脚本 `scripts/guided-playtest.mjs`。
- CUA Chrome 自然实玩：首入说明 → 双警练习，故意等第一警到点后慢速换选；两人停下尚未收网时主动补令，真实获胜；正式首关自主观察地图、两警封口、一警推进；暂停/重温/返回。无注入游戏状态或解锁。自然实玩的完成证据限于练习，正式关成功由上述测试输入回归覆盖。
- 截图和 JSON 在忽略目录 `artifacts/`，如 `guided-390.png`、`guided-report.json`、`browser-report.json`。这都是 Windows Chrome 与手机尺寸/触摸模拟，**不是手机真机**。
- 生命周期回归 10 项通过，`errors=[]`，报告时间 `2026-09-21T18:50:37.665Z`。触摸取消、无效目标不覆盖旧路线、失焦暂停、真实页面返回缓存恢复、损坏/旧存档、48 关真实触摸输入获胜及加载后离线通过。末关用时 39.9917 秒、移动模拟中位 60 FPS；末关使用解锁夹具和策略输入，不能当自然玩家或真机帧率证据。
- `git diff --check` 通过；JavaScript 项目无 TypeScript 类型检查入口。

## 未通过项、上线条件与爆款验证

| 环境 | 本轮结论 | 最少待补证据 |
| --- | --- | --- |
| 桌面独立 Web | 当前静态产物可运行，真实输入和生命周期回归 | 独立评审、代表玩家首局观察 |
| Android H5 | 手机尺寸模拟通过；真机未验证 | 一台实际目标 Android 浏览器：进入/退出全屏、暂停失败重试、旋转、安全区与工具栏变化 |
| iOS H5 | 未验证真实 API 能力与降级 | 一台 iPhone Safari：点击全屏后的真实反馈、降级下完整游玩、旋转/后台恢复 |
| Shell 聚合/iframe | 由总负责人集成验证 | 当前集成版本完整游戏/HUD/弹窗全屏与返回 |
| 小游戏平台 | 未声明已支持原生，未验收 | 如确定平台再明确账号、适配与产物，不把 H5 替代真机原生 |

下一独立评审应重点复现：暂停时处于移动命令中 → 重温 → 提前返回/胜利后返回；误按留守后的教学恢复；窄竖屏五警/六偷辨认；真实无辅助的首关及第一个换防关。未发现需要再堆功能才能解除的确定性阻断，9.5 的主要缺口是目标用户和真实设备证据。

爆款潜力需要分开检验：路网封口与实时换防具有辨识度，短视频可演示“尾追失败、封口成功”；目标轻策略玩家是否立刻感到掌控感仍是假设。复玩理由是挑战星和更快路线，分享理由目前只是展示巧妙围捕，不存在已验证邀请循环。48 关能支持一段内容，但不能据此声称长期留存。

最小实验：找 5 名未接触游戏的目标玩家，在各自手机上不解释规则观察“练习 → 首关 → 再试/下一关”，记录首次有效封口时间、求助点、是否理解到点停下、失败原因复述；次日只询问是否自发回来及原因。随后用同一来源小流量比较“直接追逐”和“封出口”两种短视频引流，复用现有 Shell 统计查看进入、首局完成和重试，结合访谈决定换防教学/分享投入。样本、满意度、次日回访与转化均应实采，本轮未采集、不设虚构结果。

---

# 别跑！街区围捕 — 2026-09-21 首轮优化交接

待独立 agent 按统一量表评分；实现者不把自己的预估分当验收分。

## 已实现

- 可选双警夹击练习：选 1 号、向蓝圈推进，换 2 号从另一侧推进，观察真实 0.8 秒收网。使用正式引擎的速度、碰撞、出口和胜负规则；双方出口初始有人把守，玩家可安心学习。完成练习仅记住“已练习”，不会解锁关卡或写入最佳用时。准备页、帮助、暂停页均有入口，可退出回到原关。
- 捕获提示直接复用模拟的 `captureStatus`：缺同伴时显示近身人数；已有两人但包围未闭合时，高亮小偷仍能到达、且超出抓捕范围的真实道路段；条件满足时显示收网百分比。多小偷时优先提示正在翻越、正在收网或离所选警察最近的目标。
- 提示栏有独立空间，不盖住地图底部出口。去掉遮挡开局道路的长提示浮层。竖屏“原地守住”图标补无障碍名称。
- 正式关准备页显示已有个人最佳；失败解释明确失守出口、实际翻越时长、无人拦截原因和重试方向。

48 张正式地图、7 个换防关、协作捕获范围、速度、挑战用时均未调整。没有引入依赖或网络服务。

## 验证

在仓库根目录执行：

```powershell
pnpm --filter cops-robbers-realtime test
pnpm --filter cops-robbers-realtime test:levels
pnpm --filter cops-robbers-realtime build
pnpm --filter cops-robbers-realtime dev
# 服务运行后，另一个终端：
pnpm --filter cops-robbers-realtime test:browser
pnpm --filter cops-robbers-realtime test:lifecycle
```

- 核心测试 21/21 通过。新增检查验证真实可退避道路段、单次练习命令不能取胜、第二人靠近后出现锁定并完成捕获。
- 关卡回放 48/48 通关，48/48 无人指挥会失守；198/198 单警尾追对照失败。使用真实 `commandCop` / `stepGame`，未注入胜负。
- 浏览器回归 15 项通过，零运行错误：真实逃脱与重试、点选/拖动/键盘、保存与重载、844×390 触摸通关、390px 竖屏与 320px 无横向溢出、第 18 关真实换防。
- 生命周期回归 10 项通过，零运行错误：触控取消、无效目标不覆盖路线、失焦暂停、真实返回缓存后显式恢复、最终关 5 警/6 偷真实触摸通关、加载完离线可玩。最终关手机模拟中位 60 FPS；这是本机模拟数据。
- CUA 独立 Chrome 实玩：桌面和 390×844 竖屏分别用实际点选命令完成练习；练习后仍显示 0/48 且返回第 1 关，重载显示“重温夹击”。844×390 检查了练习和正式地图提示、暂停 → 进入练习 → 暂停/继续 → 返回挑战。控制台无错误。
- 浏览器报告与截图在忽略的 `artifacts/` 目录；构建输出 `dist/`。

## 独立复评入口与边界

服务已启动：`http://127.0.0.1:43690`，由本游戏 `scripts/serve.mjs` 提供。新浏览器准备页点“先练夹击”，完成后进入正式关。调试的只读快照仍为 `src/main.js` 的 `getSnapshot`，不可用它伪造操作或成绩。

评分沿用核心玩法 30%、首局清晰度 20%、移动体验与反馈 20%、重玩价值 15%、完整稳定 15%；加权需达 8/10，任何维度不能低于 6。

尚未在实体 Android/iPhone 上验证，也未部署公网新版本。练习只演示双警夹击，复杂岔路和换防仍须在正式关学习；48 关挑战星与个人纪录沿用现有设计，没有添加社交排行或每日任务。

## 2026-09-21 第二轮：出口告警可操作性

本节是当前轮新增证据，上文为首轮记录。已有双警教学、抓捕诊断、失败回看仍保留，不重复开发。

**本轮前 / 后预估均为 8.2 / 10**：核心玩法 8.4、首局清晰度 8.0、移动体验与反馈 8.0、重玩价值 8.0、完整稳定 8.3；按 30% / 20% / 20% / 15% / 15% 加权为 8.165。出口提示的小幅改善不足以证明整体用户评分提升，因此没有人为抬分；仍须由新的独立评审确认。

### 新发现与修改

- 在 390×844 触摸模拟中正常开始首关、不下令，真实出现出口 A 翻越。暂停时快照显示翻越进度 0.1736，顶部却只写“正在翻越 · 快拦住！”，下方也没说哪个出口。对于多出口关卡，玩家必须自行寻找告警位置。
- 顶部现显示实际出口编号与距逃脱的剩余秒数，如“出口 A 翻越 · 剩 1.1 秒”；下方指明同一出口和靠近打断的操作。多人翻越时优先报告进度最高的目标。时间取自真实模拟，暂停时保持不变；不改变速度、翻越时限、警察数量或胜负条件。
- 复用现有出口顺序，紧急提示和失败结算共用编号函数。没有加入新系统、外部服务或依赖。

### 本轮实际检查

在游戏目录执行，预览为当前 `dist`：

```powershell
node --test tests/*.test.mjs
node scripts/verify-levels.mjs
node scripts/build.mjs
$env:PORT='43102'; node scripts/serve.mjs --dist
# 另一个终端：
$env:GAME_URL='http://127.0.0.1:43102'
node scripts/playtest.mjs
node scripts/lifecycle-playtest.mjs
```

- 核心 21/21、真实策略回放 48/48 通关、无人指挥 48/48 失守、单警尾追 198/198 失败；构建通过。
- 生命周期 10 项通过，`errors=[]`；真实触摸完成末关 5 警 / 6 偷、取消操作、失焦暂停、页面返回缓存恢复、存档兼容及加载后离线游玩均检查。末关用时 39.425 秒，浏览器移动模拟中位 60 FPS；不是实体手机数据。解锁末关使用旧存档夹具，游玩与胜负均通过真实输入完成。
- 额外在 320×740 与 390×844 浏览器触摸模拟中制造真实翻越并检查新告警，零运行错误。320px 下告警右缘 197.34px、暂停/重开组左缘 218px，没有相互覆盖或横向溢出；已人工查看截图 `artifacts/round2-320-warning.png`，另有 `round2-390-warning.png`。
- 浏览器回归 16 项通过，`errors=[]`（本轮报告时间 `2026-09-21T02:04:16.193Z`）。增加实际出口编号、剩余时间格式、提示一致性、暂停期间倒计时冻结断言，全部使用读取快照与真实按钮操作，不注入胜负；同时通过触摸练习、首两关、失败回看/重开、保存重载、第 18 关实际换防和窄屏布局检查。
- 本轮构建 `dist/src/main.js` SHA-256：`6A421C715404E85C11EA09EA964EED4D27D400AA28FDC928F4A5D5EE32C03520`。

### 距离爆款仍缺的证据与后续方向

8.2 是本地体验预估，真实用户平均评分未知。首要验证是陌生玩家能否从直路练习迁移到岔路与换防，记录首关完成率、失败后再试率、各关退出位置和次日回访；依据数据决定是否给首个换防关增加短教学。实体小屏手机的复杂地图辨认与连续点选尚未验证。48 关与个人计时提供现有重玩目标，分享挑战、长期内容更新是否值得投入，应等真实完成和回访数据再决定。
